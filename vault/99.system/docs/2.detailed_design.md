# 詳細設計

> 前提：Dataview プラグインによる動的索引。スクリプト/DB 不要。
> Markdown をセクション構成で記述し、Dataview クエリで自動抽出。

---

## 0. 用語・前提

### 用語

- **Vault**：Obsidian で開くルートフォルダ（Markdown 保管庫）
- **Daily Note**：`00.daily/YYYY-MM-DD.md` の日次ノート
- **セクション**：見出し（`## Learnings` 等）で区切られた領域
- **Dataview**：Obsidian プラグイン。Markdown からデータを抽出してクエリ表示
- **索引ページ**：`01.index/*.md` の Dataview クエリを含むノート

### 想定 OS/実行環境

- macOS
- Obsidian（Dataview プラグイン必須）
- VS Code + GitHub Copilot（編集用）

---

## 1. フォルダ/ファイル設計（確定）

```
vault/
  00.daily/
    YYYY-MM-DD.md              # 日次ノート
    Templates/
      Daily Note Simple.md     # テンプレート
  01.index/
    00.dashboard.md            # 全体概要
    01.learnings.md            # 学び一覧
    02.ideas.md                # アイディア一覧
    03.actions.md              # アクション一覧
    04.experiments.md          # 実験一覧
    books/                     # 書籍別索引（任意）
    courses/                   # コース別索引（任意）
  02.blog/
    drafts/                    # ブログ下書き
    published/                 # 公開済み
  03.ideas/
    Templates/
  99.system/
    docs/
      tag-taxonomy.md          # タグ体系
      0.requirements.md        # 要件定義
      1.outline_design.md      # 概要設計
      2.detailed_design.md     # 本ドキュメント
      3.impl_steps.md          # 実装ステップ
      4.obsidian_setup.md      # セットアップ手順
      5.ai-instructions.md     # AI向け規約
```

---

## 2. Daily Note 詳細仕様

### 2.1 ファイル命名規則

- パス：`00.daily/YYYY-MM-DD.md`
- 例：`00.daily/2025-12-21.md`

### 2.2 Frontmatter

```yaml
---
date: YYYY-MM-DD
---
```

- date のみ必須（シンプル運用）

### 2.3 セクション構成（必須）

| セクション  | 見出し           | 形式                                  | 用途                         |
| ----------- | ---------------- | ------------------------------------- | ---------------------------- |
| Summary     | `## Summary`     | 自由テキスト（1-2 行）                | 今日のハイライト             |
| Learnings   | `## Learnings`   | リスト形式（`- 項目 #tag`）           | 学んだこと                   |
| Ideas       | `## Ideas`       | チェックボックス（`- [ ] 項目 #tag`） | 思いついたこと（タスク管理） |
| Actions     | `## Actions`     | チェックボックス（`- [ ] 項目`）      | 次にやること                 |
| Experiments | `## Experiments` | 見出し形式（`### 🧪 タイトル`）       | 試したこと                   |
| Notes       | `## Notes`       | 自由テキスト                          | 詳細メモ                     |

### 2.4 リスト項目のフォーマット（Learnings）

```
- 項目テキスト #tag1 #tag2
```

- 項目テキストは自由（改行不可）
- タグは 0 個以上（複数可）
- タグは標準タグリスト（tag-taxonomy.md）から選定

### 2.5 Ideas / Actions のフォーマット

```
- [ ] やること #tag
- [x] 完了したこと #tag
```

- チェックボックス形式
- 未完了：`- [ ]`
- 完了済：`- [x]`
- Ideas と Actions の両方でタスク管理

### 2.6 Experiments のフォーマット

```
### 🧪 実験タイトル

**目的**:

**結果**:
```

- 見出し形式（`### 🧪` で始める）
- 目的と結果を記載
- Dataview で見出しベースで抽出

---

## 3. タグ仕様

### 3.1 タグ形式

- シンプル形式：`#value`
- 例：`#ai`、`#playwright`、`#reading`

### 3.2 標準タグリスト（約 20 種類）

tag-taxonomy.md で定義：

| カテゴリ     | タグ                                        |
| ------------ | ------------------------------------------- |
| AI・自動化   | #ai, #playwright, #cli                      |
| 技術・ツール | #ruby, #rails, #python, #git, #obsidian     |
| 学習         | #reading, #course                           |
| テーマ       | #productivity, #workflow, #testing, #design |
| アウトプット | #blog, #talk                                |
| コンテキスト | #work, #personal, #event                    |

### 3.3 タグ運用ルール

1. 既存タグから選定することを優先
2. 新しいタグが必要な場合は tag-taxonomy.md に追加してから使用
3. 週次/月次で類似タグを統合

---

## 4. Dataview クエリ詳細仕様

### 4.1 クエリの基本構造

```dataview
LIST L.text
FROM "00.daily"
WHERE !contains(file.path, "Templates")
FLATTEN file.lists AS L
WHERE meta(L.section).subpath = "SectionName"
SORT file.name DESC
```

### 4.2 セクション判定

- `meta(L.section).subpath` でセクション名を取得
- 見出し（`## Learnings` 等）の後に続くリスト項目を抽出

### 4.3 索引ページ別クエリ

#### learnings.md

```dataview
LIST L.text
FROM "00.daily"
WHERE !contains(file.path, "Templates")
FLATTEN file.lists AS L
WHERE meta(L.section).subpath = "Learnings"
SORT file.name DESC
```

#### ideas.md

```dataview
LIST L.text
FROM "00.daily"
WHERE !contains(file.path, "Templates")
FLATTEN file.lists AS L
WHERE meta(L.section).subpath = "Ideas"
SORT file.name DESC
```

#### actions.md（未完了のみ）

```dataview
TASK
FROM "00.daily"
WHERE !completed
WHERE !contains(file.path, "Templates")
SORT file.name DESC
```

#### experiments.md

```dataview
LIST L.text
FROM "00.daily"
WHERE !contains(file.path, "Templates")
FLATTEN file.lists AS L
WHERE meta(L.section).subpath = "Experiments"
SORT file.name DESC
```

### 4.4 タグ絞り込みクエリ

```dataview
LIST L.text
FROM "00.daily"
WHERE !contains(file.path, "Templates")
FLATTEN file.lists AS L
WHERE meta(L.section).subpath = "Ideas"
WHERE contains(L.text, "#ai")
SORT file.name DESC
```

### 4.5 統計クエリ（dashboard.md 用）

```dataview
TABLE length(rows) AS "件数"
FROM "00.daily"
WHERE !contains(file.path, "Templates")
FLATTEN file.lists AS L
WHERE meta(L.section).subpath
GROUP BY meta(L.section).subpath AS Section
```

---

## 5. テンプレート仕様

### 5.1 Daily Note Simple.md

```markdown
---
date: { { date:YYYY-MM-DD } }
---

## Summary

今日のハイライトを 1 行で

## Learnings

-

## Ideas

-

## Actions

- [ ]

## Experiments

-

## Notes
```

---

## 6. 運用フロー詳細

### 6.1 日次作成フロー

1. VS Code で `00.daily/YYYY-MM-DD.md` を作成
2. テンプレートまたは AI で内容を生成
3. 各セクションに項目を記入（タグ付き）
4. 保存 → Dataview が自動で索引を更新

### 6.2 索引確認フロー

1. Obsidian で `01.index/` を開く
2. 各索引ページで Dataview クエリが動的に一覧を表示
3. タグパネルで分類別にアクセス

### 6.3 アクション管理フロー

1. `01.index/03.actions.md` で未完了アクションを確認
2. 完了したら元の Daily Note でチェックを付ける
3. actions.md の一覧から自動的に消える

---

## 7. 制約・注意事項

### 7.1 Dataview の制約

- リアルタイム更新：ファイル保存時に反映
- 大量データ：数百件程度なら問題なし
- 複雑なクエリ：パフォーマンスに注意

### 7.2 セクション名の制約

- 見出しは正確に `## Learnings` 等と記述
- スペルミスがあると Dataview で抽出されない

### 7.3 タグの制約

- タグ内に空白は不可
- 日本語タグは可能だが英語推奨（検索性）
