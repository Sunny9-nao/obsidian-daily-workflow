# 📊 Activity Dashboard

_Last updated: Auto-generated by Dataview_

---

## 🗓️ Recent Activity (Last 7 Days)

```dataview
TABLE WITHOUT ID
  date as "📅 Date",
  learnings as "📚",
  ideas as "💡",
  actions as "✅",
  experiments as "🧪",
  (learnings + ideas + actions + experiments) as "📊 Total"
FROM "00.daily"
WHERE date >= date(today) - dur(7 days)
SORT date DESC
```

---

## 📈 Weekly Summary

```dataview
TABLE WITHOUT ID
  sum(rows.learnings) as "📚 Learnings",
  sum(rows.ideas) as "💡 Ideas",
  sum(rows.actions) as "✅ Actions",
  sum(rows.experiments) as "🧪 Experiments",
  sum(rows.learnings) + sum(rows.ideas) + sum(rows.actions) + sum(rows.experiments) as "📊 Total"
FROM "00.daily"
WHERE date >= date(today) - dur(7 days)
```

---

## 🏆 Most Productive Days (Top 10)

```dataview
TABLE WITHOUT ID
  date as "📅 Date",
  (learnings + ideas + actions + experiments) as "📊 Total Activity",
  learnings as "📚",
  ideas as "💡",
  actions as "✅",
  experiments as "🧪"
FROM "00.daily"
WHERE (learnings + ideas + actions + experiments) > 0
SORT (learnings + ideas + actions + experiments) DESC
LIMIT 10
```

---

## 📚 Recent Learnings

```dataview
LIST
FROM "00.daily"
WHERE contains(file.content, "[learning]")
SORT date DESC
LIMIT 10
```

---

## 💡 Recent Ideas

```dataview
LIST
FROM "00.daily"
WHERE contains(file.content, "[idea]")
SORT date DESC
LIMIT 10
```

---

## ✅ Pending Actions

```dataview
TABLE WITHOUT ID
  date as "📅 Date",
  regexreplace(text, ".*\[action\]\s*(.*?)\s*status=.*", "$1") as "Task"
FROM "00.daily"
WHERE contains(file.content, "[action]") AND contains(file.content, "status=todo")
SORT date DESC
```

---

## 🧪 Recent Experiments

```dataview
TABLE WITHOUT ID
  date as "📅 Date",
  regexreplace(text, ".*\[experiment\]\s*(.*?)\s*#.*", "$1") as "Experiment"
FROM "00.daily"
WHERE contains(file.content, "[experiment]")
SORT date DESC
LIMIT 10
```

---

## 🏷️ Activity by Tag

### Tech Tags

```dataview
TABLE WITHOUT ID
  rows.file.link as "Daily Notes"
FROM "00.daily"
WHERE contains(file.content, "#tech:")
GROUP BY file.tags
SORT length(rows) DESC
```

### Topics

```dataview
TABLE WITHOUT ID
  rows.file.link as "Daily Notes"
FROM "00.daily"
WHERE contains(file.content, "#topic:")
GROUP BY file.tags
SORT length(rows) DESC
```

---

## 📊 Monthly Overview

```dataview
TABLE WITHOUT ID
  dateformat(date, "yyyy-MM") as "Month",
  sum(rows.learnings) as "📚",
  sum(rows.ideas) as "💡",
  sum(rows.actions) as "✅",
  sum(rows.experiments) as "🧪"
FROM "00.daily"
GROUP BY dateformat(date, "yyyy-MM")
SORT dateformat(date, "yyyy-MM") DESC
```

---

_💡 Tip: このダッシュボードはデイリーノートの frontmatter と INDEX セクションから自動生成されます_
